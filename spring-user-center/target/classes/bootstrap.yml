server:
  port: 8033

spring:
  application:
    name: user-center
  profiles:
    active: test
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        file-extension: yaml


